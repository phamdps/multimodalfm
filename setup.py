# setup.py
from setuptools import setup, find_packages
setup(name="multimodalfm", version="0.1", packages=find_packages())

# Once created, run `pip install -e .` in your terminal. 
# This allows you to simply use from src.model import SimpleModel inside your scripts
# without manually modifying the path every time!